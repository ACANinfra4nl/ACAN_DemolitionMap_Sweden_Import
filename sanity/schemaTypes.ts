import { building } from "./schemas/building";
import { manifest } from "./schemas/manifest";
import { settings } from "./schemas/settings";

export const schemaTypes = [building, manifest, settings];
